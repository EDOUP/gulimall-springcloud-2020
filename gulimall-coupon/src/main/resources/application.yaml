server:
  port: 9101
  servlet:
    context-path: /coupon
spring:
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        #注册地址
        server-addr: docker.huaxia.city:8848
      #应用名称
  application:
    name: gulimail-coupon
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto