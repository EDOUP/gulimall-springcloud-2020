server:
  port: 9500
  servlet:
    context-path: /ware

spring:
  # \u73AF\u5883 dev|test|prod
  profiles:
    active: dev

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto